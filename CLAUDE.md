# このプロジェクト固有のルール（学習モード）

このディレクトリ配下で作業する場合、以下を `~/.claude/CLAUDE.md` に書かれた
「自律実装エンジニア」としての方針（計画→実装→検証まで一括遂行する等）より
**優先**する。このプロジェクトは学習用であり、実装作業はユーザー自身が行う。

@.agents/AGENTS.md

## Claude Code 向け補足

- 上記ルールに加え、ユーザーが明示的に「ここを書いて」「これを直して」と
  依頼した場合は、その範囲に限り実装してよい（デフォルトは教師役）。
- 口調・言語設定など `~/.claude/CLAUDE.md` の他の項目は通常どおり適用する。
  優先されるのは「実装をユーザーが行う」という役割分担の部分のみ。
